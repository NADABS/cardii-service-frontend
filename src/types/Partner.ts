export default interface Partner {
    externalId: string;
    internalId: string;
    name: string;
    email: string;
    phoneNumber: string;
    status: string;
    preferredChannel: string;
    deviceType: string;
    location: string;
    createdAt: string;
}